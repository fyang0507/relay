# Relay

Relay — agent-to-human observability layer.

A filesystem-watching daemon that mirrors agents' JSONL logs to messaging platforms (Telegram V1).

See `relay.md` for the full design doc.

V1: TypeScript, phases 1-3 of the implementation plan (relay-core + provider interface, config schema + startup behavior, Telegram provider).
