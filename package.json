{
  "name": "relay",
  "version": "1.2.0",
  "type": "module",
  "bin": {
    "relay": "dist/cli.js",
    "relayd": "dist/daemon.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && chmod +x dist/cli.js dist/daemon.js && node scripts/sync-skills.mjs",
    "test": "node --test 'tests/**/*.test.ts'"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "chokidar": "^4.0.3",
    "commander": "^14.0.3",
    "dotenv": "^17.4.0",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2"
  }
}
